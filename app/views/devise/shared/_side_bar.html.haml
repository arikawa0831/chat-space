.chat__side__top
  .chat__side__top__user-name
    = current_user.name
  .chat__side__top__icon
    = link_to new_group_path do
      = fa_icon 'pencil-square-o'
    = link_to edit_user_path(current_user) do
      = fa_icon 'cog'


.chat__side__glist 
  -current_user.groups.each do |group| 
  .chat__side__glist__group  
    = link_to '#' do
    .chat__side__glist__group__name
      = group.name
    .chat__side__glist__group__message
      メッセージはまだありません。
